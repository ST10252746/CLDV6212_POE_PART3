@page
@model RegisterModel
@{
    ViewData["Title"] = "Register";
}

<div class="register-container">
    <div class="register-box">
        <div class="register-header">
            <i class="fas fa-user-plus registerpage-icon "></i>
            <h1 class="register-title">Create a New Account</h1>
        </div>
        <form id="registerForm" asp-route-returnUrl="@Model.ReturnUrl" method="post" class="form-container">
            <div asp-validation-summary="ModelOnly" class="text-danger validation-summary" role="alert"></div>

            <div class="form-group">
                <label asp-for="Input.Firstname" class="form-label">First Name</label>
                <input asp-for="Input.Firstname" class="form-control" autocomplete="firstname" placeholder="Enter your first name" aria-required="true" />
                <span asp-validation-for="Input.Firstname" class="text-danger"></span>
            </div>

            <div class="form-group">
                <label asp-for="Input.Lastname" class="form-label">Last Name</label>
                <input asp-for="Input.Lastname" class="form-control" autocomplete="lastname" placeholder="Enter your last name" aria-required="true" />
                <span asp-validation-for="Input.Lastname" class="text-danger"></span>
            </div>

            <div class="form-group">
                <label asp-for="Input.Email" class="form-label">Email</label>
                <input asp-for="Input.Email" class="form-control" autocomplete="username" placeholder="name@example.com" aria-required="true" />
                <span asp-validation-for="Input.Email" class="text-danger"></span>
            </div>

            <div class="form-group">
                <label asp-for="Input.Password" class="form-label">Password</label>
                <input asp-for="Input.Password" class="form-control" autocomplete="new-password" placeholder="Enter your password" aria-required="true" />
                <span asp-validation-for="Input.Password" class="text-danger"></span>
            </div>

            <div class="form-group">
                <label asp-for="Input.ConfirmPassword" class="form-label">Confirm Password</label>
                <input asp-for="Input.ConfirmPassword" class="form-control" autocomplete="new-password" placeholder="Re-enter your password" aria-required="true" />
                <span asp-validation-for="Input.ConfirmPassword" class="text-danger"></span>
            </div>

            <div class="form-group">
                <label asp-for="Input.Role" class="form-label">Select Role</label>
                <select asp-for="Input.Role" asp-items="@Model.Input.RoleList" class="form-select" aria-required="true">
                    <option disabled selected>Select a role</option>
                </select>
                <span asp-validation-for="Input.Role" class="text-danger"></span>
            </div>

            <button id="registerSubmit" type="submit" class="btn-submit">Register</button>
        </form>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}

<style>
    body {
        font-family: 'Helvetica', sans-serif;
        background-color: #524da0;
        color: #333;
    }

    .register-container {
        display: flex;
        margin-top: 80px; /* Reduced */
        justify-content: center;
        align-items: center;
        height: 100vh;
        gap: 40px; /* Reduced */
        margin-bottom: 40px; /* Reduced */
    }

    .register-box {
        background-color: #ffffff;
        padding: 40px; /* Reduced */
        border-radius: 10px;
        box-shadow: 0 3px 10px rgba(0, 0, 0, 0.15); /* Slightly reduced */
        max-width: 360px; /* Reduced */
        width: 100%;
        transition: transform 0.3s ease, box-shadow 0.3s ease;
    }

        .register-box:hover {
            transform: translateY(-4px); /* Reduced */
            box-shadow: 0 5px 12px rgba(0, 0, 0, 0.2); /* Slightly reduced */
        }

    .register-header {
        text-align: center;
        margin-bottom: 24px; /* Reduced */
        position: relative;
    }

    .registerpage-icon {
        font-size: 48px; /* Reduced */
        color: #531b6a;
        display: block;
        margin-bottom: 12px; /* Reduced */
    }

    .register-title {
        font-size: 19px; /* Reduced */
        font-weight: bold;
        color: #531b6a;
        display: block;
        margin-left: 8px; /* Reduced */
    }

    .form-group {
        margin-bottom: 16px; /* Reduced */
    }

    .form-label {
        color: #531b6a;
        font-weight: 600;
        font-size: 13px; /* Reduced */
    }

    .form-control {
        width: 100%;
        padding: 10px; /* Reduced */
        font-size: 12px; /* Reduced */
        border-radius: 5px;
        border: 1px solid #ced4da;
        transition: border-color 0.2s ease;
    }

        .form-control:focus {
            border-color: #531b6a;
            box-shadow: 0 0 4px rgba(38, 36, 89, 0.8); /* Slightly reduced */
        }

    .form-select {
        width: 100%;
        padding: 8px; /* Reduced */
        border-radius: 5px;
        border: 1px solid #ced4da;
        font-size: 12px; /* Reduced */
        transition: border-color 0.2s ease;
    }

        .form-select:focus {
            border-color: #531b6a;
            box-shadow: 0 0 4px rgba(38, 36, 89, 0.8); /* Slightly reduced */
        }

    .btn-submit {
        width: 100%;
        padding: 10px; /* Reduced */
        background-color: #531b6a;
        color: white;
        font-size: 14px; /* Reduced */
        border: 1px solid #531b6a;
        border-radius: 5px;
        cursor: pointer;
        transition: background-color 0.3s ease, transform 0.2s ease;
    }

        .btn-submit:hover {
            background-color: #222268;
            border-color: #222268;
            transform: scale(1.02); /* Same */
        }

    .text-danger {
        font-size: 12px; /* Reduced */
    }
</style>
